# Plugin Documentation

The general plugin ecosystem is outside Phase 01 scope. The Docker-based Pi-hole appliance establishes only a deployment and route namespace; it is not yet a Sovereign plugin or capability provider.

Future documentation will cover manifests, lifecycle, permissions, health, configuration, data ownership, SDK usage, packaging, signing, provenance, compatibility, review, and removal.

No external plugin API should be described as stable during the preview.
