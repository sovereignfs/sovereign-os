#Type Name                     ID GECOS Home directory Shell
# Deliberately not named "sovereign-conversation" -- systemd's
# DynamicUser derives a same-named dynamic group from a unit's own name,
# and sovereign-conversation.service claims that exact identity. Named
# after what it protects (the Pi-hole secret), not the one consumer that
# currently needs it, matching sovereign-console-secrets's own precedent
# and the same real "group name collided with a DynamicUser identity"
# failure mode found on real hardware for that group.
g     sovereign-pihole-secrets -
