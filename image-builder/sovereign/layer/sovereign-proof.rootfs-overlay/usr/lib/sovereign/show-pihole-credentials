#!/bin/sh

set -eu

password_file=/data/sovereign/secrets/pihole-admin-password
password=$(tr -d '\n' < "$password_file")

printf '\n'
printf 'Sovereign OS is ready.\n'
printf 'Pi-hole:  http://sovereign.local/dns/admin/\n'
printf 'Fallback: http://<device-ip>/dns/admin/\n'
printf 'Password: %s\n' "$password"
printf 'This credential is shown only on the attached physical console.\n'
printf '\n'
