#!/bin/sh

set -eu

compose_file=/data/sovereign/apps/pihole/compose.yaml

if [ -f "$compose_file" ]; then
  docker compose \
    --project-name sovereign-pihole \
    --file "$compose_file" \
    stop --timeout 30
fi
